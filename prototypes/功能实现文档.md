# 个人门户网站功能实现文档

## 一、实现范围

- 实现单页个人门户原型，覆盖首页、作品、文章、工具箱四个主视图。
- 实现首页公开展示文案，包括个人定位、信息标签、价值陈述、CTA 和能力摘要。
- 实现作品列表、作品分类筛选和作品详情二级页面。
- 实现基于 2023 UI/UX 作品集的图文案例展示，包括作品封面、本地图片资产、标签、关键指标和结构化详情。
- 实现周一至周五固定晨读封面列表和文章详情二级页面，晨读封面使用 `assets/reading/` 本地图片资产，详情页支持列表摘要标题、日期、趋势描述、标签元信息和详细内容。
- 实现工具箱分类资源列表和外链打开。
- 实现联系抽屉，用于承接面试、产品设计、AI 原型和合作机会的联系说明。
- 实现桌面端和移动端基础响应式布局。
- 实现基础可访问性能力，包括焦点样式、Enter / Space 进入文章详情、详情页返回后焦点恢复、Escape 关闭联系抽屉和焦点恢复。

## 二、核心状态

| 状态/数据 | 用途 |
| --- | --- |
| `panels` | 保存所有主视图节点，用于控制首页、作品、文章、工具箱的显示状态 |
| `navButtons` | 保存顶部导航按钮，用于同步当前视图高亮 |
| `caseAssets` | 保存作品图片资产路径，用于统一引用本地化后的 Figma 截图 |
| `cases` | 保存作品数据，包含类型、标签、标题、摘要、封面图、详情图、关键指标、结构化说明和结果沉淀 |
| `readingAssets` | 保存周一至周五晨读封面图片路径，用于文章列表左侧海报展示 |
| `blogPosts` | 保存晨读文章数据，包含工作日、封面图片、标题、列表摘要、今日晨读描述、日期、趋势标题、趋势摘要、主题标签、阅读时长、Markdown 详细内容或结构化 sections |
| `resources` | 保存工具箱资源数据，按设计、产品、AI、研发四类组织 |
| `caseDetailType` | 保存作品详情页右上角类型标签节点 |
| `caseDetailLabel` | 保存作品详情页标题上方类型标签节点 |
| `caseDetailTitle` | 保存作品详情页标题节点 |
| `caseDetailBody` | 保存作品详情页摘要正文节点 |
| `caseDetailResult` | 保存作品详情页项目结果节点 |
| `caseDetailContent` | 保存作品详情页图文详情内容节点 |
| `backToPortfolioBtn` | 保存作品详情页返回按钮节点 |
| `lastPortfolioFocused` | 记录打开作品详情页前的作品卡片焦点，用于返回作品列表后恢复焦点 |
| `articleDetailType` | 保存文章详情页右上角主题标签节点 |
| `articleDetailLabel` | 保存文章详情页标题上方主题标签节点 |
| `articleDetailTitle` | 保存文章详情页列表摘要标题节点 |
| `articleDetailBody` | 保存文章详情页趋势标题节点 |
| `articleDetailMeta` | 保存文章详情页日期节点 |
| `articleDetailContent` | 保存文章详情页正文内容节点 |
| `backToBlogBtn` | 保存文章详情页返回按钮节点 |
| `lastBlogFocused` | 记录打开文章详情页前的晨读封面卡片焦点，用于返回文章列表后恢复焦点 |
| `detailDrawer` | 保存联系抽屉节点，用于承载联系信息 |
| `drawerTitle` | 保存抽屉标题节点 |
| `drawerBody` | 保存抽屉正文节点 |
| `drawerExtra` | 保存抽屉补充内容节点 |
| `lastFocused` | 记录打开抽屉前的焦点元素，用于关闭抽屉后恢复焦点 |
| `contactDrawerContent` | 保存联系抽屉内容，包括说明文案、邮箱和外部平台入口 |
| `renderTagList()` | 将作品标签数据渲染为卡片标签和详情页标签 |
| `renderCaseDetail()` | 将作品详情图、图片说明、关键指标和结构化章节渲染到作品详情页 |
| `openCaseDetail()` | 将当前作品数据写入详情页，并切换到作品详情二级页面 |
| `backToPortfolio()` | 从作品详情页返回作品列表，并恢复作品卡片焦点 |
| `escapeHtml()` | 对详情页动态文本进行 HTML 转义，避免正文内容破坏页面结构 |
| `renderSafeTagList()` | 将文章标签和阅读时长渲染为详情页标签 |
| `markdownToHtml()` | 将可选 Markdown 正文转换为文章详情页富文本结构 |
| `renderArticleSections()` | 将文章分段分析数据渲染为详情页结构化正文 |
| `renderArticleDetail()` | 将趋势标题、趋势摘要、主题标签、阅读时长和详细内容渲染到文章详情页 |
| `openArticleDetail()` | 将当前文章数据写入详情页，并切换到文章详情二级页面 |
| `backToBlog()` | 从文章详情页返回文章列表，并恢复晨读封面卡片焦点 |

## 三、数据流

### 1. 初始化数据流

- 页面加载后读取内置的 `caseAssets`、`readingAssets`、`cases`、`blogPosts`、`resources` 和 `contactDrawerContent`。
- 调用 `renderCases()` 渲染全部作品。
- 调用 `renderBlog()` 渲染晨读封面列表。
- 调用 `renderResources()` 渲染默认设计分类资源。
- 首页视图默认带有 `active` 状态，因此首屏直接展示首页内容。

### 2. 视图切换数据流

- 用户点击带有 `data-view` 的按钮后，读取按钮上的目标视图名称。
- `showView(name)` 遍历所有主视图节点，根据 `data-panel` 与目标视图是否一致切换 `active` 状态。
- `showView(name)` 同步遍历顶部导航按钮，根据 `data-view` 与目标视图是否一致切换高亮状态。
- 当目标视图为 `portfolio-detail` 时，顶部导航仍将“作品”保持为高亮状态。
- 当目标视图为 `article-detail` 时，顶部导航仍将“文章”保持为高亮状态。

### 3. 作品数据流

- 作品数据保存在 `cases` 数组中。
- 作品图片路径保存在 `caseAssets` 对象中，图片文件位于 `assets/portfolio/` 目录。
- 用户点击筛选项后，读取筛选按钮的 `data-filter`。
- `renderCases(type)` 根据筛选类型过滤 `cases`。
- 过滤后的作品数据被渲染为卡片按钮，每张卡片包含封面图、类型、标题、摘要、标签和结果说明。
- 用户点击作品卡片后，将当前作品数据传入 `openCaseDetail(item)`。
- `openCaseDetail(item)` 记录当前焦点到 `lastPortfolioFocused`，并将作品类型、标题、详情、结果和图文内容写入作品详情页。
- `renderCaseDetail(item)` 将当前作品的详情图、图片说明、关键指标和结构化章节写入 `caseDetailContent`。
- `showView("portfolio-detail")` 切换到作品详情二级页面，同时保持顶部“作品”导航高亮。
- 用户点击“返回作品”后触发 `backToPortfolio()`，返回作品列表并恢复进入详情前的作品卡片焦点。
- 作品详情页不改变当前筛选状态。

### 4. 文章数据流

- 文章数据保存在 `blogPosts` 数组中。
- 每篇晨读文章通过 `poster` 字段引用 `readingAssets` 中的工作日封面图。
- `renderBlog()` 将 5 篇工作日晨读文章渲染为左图右文的晨读封面卡片。
- 用户点击晨读封面卡片，或在卡片聚焦时按 Enter / Space 后，将当前文章数据传入 `openArticleDetail(post)`。
- `openArticleDetail(post)` 记录当前焦点到 `lastBlogFocused`，并将文章主题标签、列表摘要标题、趋势标题、日期和正文内容写入文章详情页。
- `renderArticleDetail(post)` 将当前文章的趋势标题、趋势摘要、主题标签、阅读时长和详细内容写入 `articleDetailContent`。
- 当文章提供 `contentMarkdown` 时，`markdownToHtml()` 负责将 Markdown 正文转换为标题、段落、列表和链接；当文章提供 `sections` 时，`renderArticleSections()` 负责渲染结构化分析卡片。
- `showView("article-detail")` 切换到文章详情二级页面，同时保持顶部“文章”导航高亮。
- 用户点击“返回文章”后触发 `backToBlog()`，返回文章列表并恢复进入详情前的晨读封面卡片焦点。

### 5. 工具箱数据流

- 工具箱数据保存在 `resources` 对象中，并按分类分组。
- 用户点击工具箱分类按钮后，读取按钮的 `data-resource`。
- `renderResources(type)` 根据分类读取对应资源数组。
- 每个资源被渲染为外链卡片，外链使用新窗口打开。
- 外链打开不回写页面状态。

### 6. 联系抽屉数据流

- `openDrawer()` 记录当前焦点元素到 `lastFocused`。
- `openDrawer()` 将联系标题、正文和补充内容写入抽屉节点。
- `openDrawer()` 为抽屉添加 `open` 状态，并将 `aria-hidden` 设置为 `false`。
- `closeDrawer()` 移除抽屉 `open` 状态，并将 `aria-hidden` 设置为 `true`。
- `closeDrawer()` 在存在 `lastFocused` 时恢复焦点。
