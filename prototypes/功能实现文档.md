# 个人门户网站功能实现文档

## 一、实现范围

- 实现单页个人门户原型，覆盖首页、作品、文章、工具箱四个主视图。
- 实现首页公开展示文案，包括个人定位、信息标签、价值陈述、CTA 和能力摘要。
- 实现作品列表、作品分类筛选和作品详情二级页面。
- 实现基于 2023 UI/UX 作品集的图文案例展示，包括作品封面、本地图片资产、标签、关键指标和结构化详情。
- 实现文章列表和文章详情抽屉。
- 实现文章模块中的 Notion 晨读内容展示，包括最新晨读、历史归档和详情抽屉。
- 实现工具箱分类资源列表和外链打开，工具资源由 Notion 工具导航 CMS 同步生成。
- 实现 GitHub Actions 定时同步 Notion CMS，生成公开 JSON 内容供 GitHub Pages 静态读取。
- 实现联系抽屉，用于承接面试、产品设计、AI 原型和合作机会的联系说明。
- 实现桌面端和移动端基础响应式布局。
- 实现基础可访问性能力，包括焦点样式、Enter 打开文章详情、Escape 关闭抽屉和焦点恢复。

## 二、核心状态

| 状态/数据 | 用途 |
| --- | --- |
| `panels` | 保存所有主视图节点，用于控制首页、作品、文章、工具箱的显示状态 |
| `navButtons` | 保存顶部导航按钮，用于同步当前视图高亮 |
| `caseAssets` | 保存作品图片资产路径，用于统一引用本地化后的 Figma 截图 |
| `cases` | 保存作品数据，包含类型、标签、标题、摘要、封面图、详情图、关键指标、结构化说明和结果沉淀 |
| `blogPosts` | 保存站点内置文章数据，包含标题、摘要、主题标签、阅读时长和详情 |
| `readingEndpoints` | 保存晨读公开 JSON 路径，用于读取最新晨读和历史索引 |
| `latestReading` | 保存文章模块内最新晨读展示容器 |
| `readingList` | 保存文章模块内晨读历史列表容器 |
| `toolEndpoint` | 保存工具导航公开 JSON 路径 |
| `resourceGroups` | 保存从工具导航 JSON 读取到的资源分组数据 |
| `caseDetailType` | 保存作品详情页右上角类型标签节点 |
| `caseDetailLabel` | 保存作品详情页标题上方类型标签节点 |
| `caseDetailTitle` | 保存作品详情页标题节点 |
| `caseDetailBody` | 保存作品详情页摘要正文节点 |
| `caseDetailResult` | 保存作品详情页项目结果节点 |
| `caseDetailContent` | 保存作品详情页图文详情内容节点 |
| `backToPortfolioBtn` | 保存作品详情页返回按钮节点 |
| `lastPortfolioFocused` | 记录打开作品详情页前的作品卡片焦点，用于返回作品列表后恢复焦点 |
| `detailDrawer` | 保存详情抽屉节点，用于统一承载文章详情和联系信息 |
| `drawerTitle` | 保存抽屉标题节点 |
| `drawerBody` | 保存抽屉正文节点 |
| `drawerExtra` | 保存抽屉补充内容节点 |
| `lastFocused` | 记录打开抽屉前的焦点元素，用于关闭抽屉后恢复焦点 |
| `contactDrawerContent` | 保存联系抽屉内容，包括说明文案、邮箱和外部平台入口 |
| `renderTagList()` | 将作品标签数据渲染为卡片标签和详情页标签 |
| `renderCaseDetail()` | 将作品详情图、图片说明、关键指标和结构化章节渲染到作品详情页 |
| `openCaseDetail()` | 将当前作品数据写入详情页，并切换到作品详情二级页面 |
| `backToPortfolio()` | 从作品详情页返回作品列表，并恢复作品卡片焦点 |
| `loadReadings()` | 读取晨读 JSON，并触发最新晨读和历史列表渲染 |
| `renderLatestReading()` | 渲染文章模块顶部的最新晨读 |
| `renderReadingList()` | 渲染晨读历史归档列表 |
| `openReading()` | 将晨读详情写入统一抽屉 |
| `loadResources()` | 读取工具导航 JSON，并写入 `resourceGroups` |

## 三、数据流

### 1. 初始化数据流

- 页面加载后读取内置的 `caseAssets`、`cases`、`blogPosts` 和 `contactDrawerContent`。
- 调用 `renderCases()` 渲染全部作品。
- 调用 `renderBlog()` 渲染文章列表。
- 调用 `loadReadings()` 读取 `content/readings/latest.json` 与 `content/readings/index.json`，渲染文章模块中的晨读内容。
- 调用 `loadResources()` 读取 `content/tools/index.json`，渲染默认设计分类资源。
- 首页视图默认带有 `active` 状态，因此首屏直接展示首页内容。

### 2. 视图切换数据流

- 用户点击带有 `data-view` 的按钮后，读取按钮上的目标视图名称。
- `showView(name)` 遍历所有主视图节点，根据 `data-panel` 与目标视图是否一致切换 `active` 状态。
- `showView(name)` 同步遍历顶部导航按钮，根据 `data-view` 与目标视图是否一致切换高亮状态。
- 当目标视图为 `portfolio-detail` 时，顶部导航仍将“作品”保持为高亮状态。

### 3. 作品数据流

- 作品数据保存在 `cases` 数组中。
- 作品图片路径保存在 `caseAssets` 对象中，图片文件位于 `assets/portfolio/` 目录。
- 用户点击筛选项后，读取筛选按钮的 `data-filter`。
- `renderCases(type)` 根据筛选类型过滤 `cases`。
- 过滤后的作品数据被渲染为卡片按钮，每张卡片包含封面图、类型、标题、摘要、标签和结果说明。
- 用户点击作品卡片后，将当前作品数据传入 `openCaseDetail(item)`。
- `openCaseDetail(item)` 记录当前焦点到 `lastPortfolioFocused`，并将作品类型、标题、详情、结果和图文内容写入作品详情页。
- `renderCaseDetail(item)` 将当前作品的详情图、图片说明、关键指标和结构化章节写入 `caseDetailContent`。
- `showView("portfolio-detail")` 切换到作品详情二级页面，同时保持顶部“作品”导航高亮。
- 用户点击“返回作品”后触发 `backToPortfolio()`，返回作品列表并恢复进入详情前的作品卡片焦点。
- 作品详情页不改变当前筛选状态。

### 4. 文章数据流

- 文章数据保存在 `blogPosts` 数组中。
- `renderBlog()` 将每篇文章渲染为文章卡片。
- 晨读内容从公开 JSON 读取，最新晨读来自 `latest.json`，历史列表来自 `index.json`。
- 用户点击晨读历史项后，页面按历史项的 `path` 读取对应归档 JSON。
- 晨读详情通过 `openReading()` 写入统一抽屉，正文 Markdown 由 `markdownToHtml()` 转为安全 HTML。
- 用户点击文章卡片或在文章卡片聚焦时按 Enter 后，将文章标题、详情、主题标签和阅读时长传入 `openDrawer()`。
- 抽屉展示文章详情，不改变当前主视图状态。

### 5. 工具箱数据流

- 工具箱数据从 `content/tools/index.json` 读取，并按设计、产品、AI、研发四类写入 `resourceGroups`。
- 用户点击工具箱分类按钮后，读取按钮的 `data-resource`。
- `renderResources(type)` 根据分类读取对应资源数组；如果当前分类无资源，则展示空状态。
- 每个资源被渲染为外链卡片，外链使用新窗口打开。
- 外链打开不回写页面状态。

### 6. 抽屉数据流

- `openDrawer()` 记录当前焦点元素到 `lastFocused`。
- `openDrawer()` 将传入的标题、正文和补充内容写入抽屉节点。
- `openDrawer()` 为抽屉添加 `open` 状态，并将 `aria-hidden` 设置为 `false`。
- `closeDrawer()` 移除抽屉 `open` 状态，并将 `aria-hidden` 设置为 `true`。
- `closeDrawer()` 在存在 `lastFocused` 时恢复焦点。
